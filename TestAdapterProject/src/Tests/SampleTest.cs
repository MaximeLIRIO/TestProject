using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace TestAdapterProject.Tests
{
    [TestClass]
    public class SampleTest
    {
        [TestMethod]
        public void TestMethod1()
        {
            // Arrange
            // (Setup code here)

            // Act
            // (Code to execute the method being tested)

            // Assert
            // (Assertions to verify the expected outcome)
        }

        [TestMethod]
        public void TestMethod2()
        {
            // Arrange
            // (Setup code here)

            // Act
            // (Code to execute the method being tested)

            // Assert
            // (Assertions to verify the expected outcome)
        }
    }
}